<div class="navbar">
    <a href="/">Home</a>
    <a href="/collections">Collections</a>
    <a href="/profile">My Profile</a>
</div>

<div class="body">
    {{#each figurines}}
    <h2>{{figurineName}}</h2>
    {{#each seriesId}}
    <p>{{this}}</p>
    {{/each}}
    {{/each}}
</div>